# Copy this file to ".env.local" (Next.js loads that automatically, and it's
# gitignored). This tells the frontend where the Express backend is running.
NEXT_PUBLIC_API_URL=http://localhost:4000

# This value is compiled into the browser bundle during `npm run build`.
# Set it to the public HTTPS backend URL in the frontend build environment.
